USE `Parks_and_Recreation`;  -- signaling to the server that we are using the database

SHOW TABLES;

-- the command above should display three tables: employee_demographics, employee_salary, parks_departments

-- let's start messing around with employee_demographics
SELECT * FROM employee_demographics LIMIT 10; 
